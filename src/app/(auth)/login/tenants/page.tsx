import TenantsHome from '@components/tenants/TenantsHome'

export default function Page() {
    return (
        <div className={"h-screen w-full flex justify-center items-center"}>
            <TenantsHome />
        </div>
    )
}